// Ce module est désormais géré par Better Auth organization. Toutes les opérations de rôle doivent passer par l'API Better Auth.
import express from 'express';
const router = express.Router();

// Voir la doc Better Auth pour la gestion des membres et rôles d'organisation.

export default router;
